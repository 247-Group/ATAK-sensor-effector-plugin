<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ATAK Plugin Lifecycle Receiver -->
        <receiver
            android:name=".plugin.SensorEffectorLifecycle"
            android:exported="true">
            <intent-filter>
                <action android:name="com.atakmap.app.PLUGIN" />
            </intent-filter>
            <meta-data
                android:name="plugin-api"
                android:value="true" />
        </receiver>

        <!-- Background service for continuous RACK communication -->
        <service
            android:name=".network.RackConnectionService"
            android:exported="false" />

    </application>

</manifest>
